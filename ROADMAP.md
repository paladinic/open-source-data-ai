improve nav
- is it all one page with different tabs? does it make sense to split into separate pages? for maintainability etc..?
- improve nav: always show dashboards / components / home

auth & storage
- supabase

model selection
- user provides keys for models and can select from available models


dashboard
- user can add include inputs like sliders, dropdowns, buttons that interact with the viz's

connectors
- airbyte

compute
- run on server?
- run on specific server?